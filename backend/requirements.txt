# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM Support =============
# OpenAI SDK (all LLM calls use the OpenAI-compatible format)
openai>=1.91.0

# ============= Graph Backends =============
zep-cloud==3.13.0
# Local Graphiti support is installed separately with:
#   uv pip install --python .venv/bin/python --no-deps graphiti-core==0.28.2
# This avoids conflicting Neo4j driver pins between Graphiti and camel-oasis.
numpy>=1.0.0
posthog>=3.0.0
tenacity>=9.0.0

# ============= OASIS Social Media Simulation =============
# OASIS social simulation framework
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports text files that are not UTF-8)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utilities =============
# Environment variable loading
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
